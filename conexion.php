<?php
$conn = new mysqli('localhost','root','','car_wash');
    // $usuario = "root";
    // $contrasena = "";
    // $servidor = "localhost:8080";
    // $basededatos = "car_wash";

    // $conn = mysqli_connect( $servidor, $usuario, $contrasena, $basededatos,'33065') or die ("No se ha podido conectar al servidor de Base de datos");
?>